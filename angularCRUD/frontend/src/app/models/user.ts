export interface User {
    id: number,
    nume: string,
    prenume: string,
    email:string,
    datanastere: Date,
    dataadaugare?: Date,
    telefon: string
}
// tipul de date cu care o sa ne jucam